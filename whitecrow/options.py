import os

ROOT = os.path.join(os.path.dirname(__file__), "..")
ANIMATION_FOLDER = os.path.join(ROOT, "data", "animations")

OPTIONS = {
    "resolution": (640, 268),
    "fps": 30,
    "grid_size": 10
}